#!/usr/bin/env ruby
# frozen_string_literal: true

root = File.expand_path('..', __dir__)
require "#{root}/lib/cli/cli"

ARGV.unshift("default") if ARGV.empty? || ARGV.first.start_with?("-")
Dry::CLI.new(AlcesJob::CLI).call